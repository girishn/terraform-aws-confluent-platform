# Changelog

All notable changes to this module are documented here. Consumers should pin to a git tag (e.g. `v1.0.0`) and upgrade when a new release is cut.

## [Unreleased]

- (Add changes here before tagging a release.)

## [1.0.0] – TBD

- Initial release as reusable module for teams.
- Terraform: VPC, EKS (with eks-pod-identity-agent), EBS CSI addon (Pod Identity), CFK operator only.
- Manifests: Zookeeper + Kafka (cp-server 7.9.0) with internal NLB for same-VPC access.
- Docs: README (consumer flow), MAINTAINING.md, manifests README.
